[package]
name = "makepad-example-notification"
version = "0.1.0"
edition = "2021"
description = "Standalone notification example for makepad-components"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { workspace = true }
makepad-components = { workspace = true }
    