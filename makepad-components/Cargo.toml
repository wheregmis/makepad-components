[package]
name = "makepad-components"
version = "1.0.0"
edition = "2021"
description = "Reusable UI components for Makepad"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { workspace = true }
makepad-icon = { workspace = true }
