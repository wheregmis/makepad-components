[package]
name = "makepad-icon"
version = "1.0.0"
edition = "2021"
description = "Reusable SVG icon set for Makepad"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { git = "https://github.com/makepad/makepad.git", branch = "dev", version = "2.0.0" }
